# Benchmarks

Cross-library JSON benchmarks for `datavalue-rs`. Compares against
`serde_json`, `simd-json`, `sonic-rs`, and `json-rust` across four workloads
— parse, serialize, access, mutate — over the canonical fixtures used by
the wider Rust JSON ecosystem (`twitter.json`, `citm_catalog.json`,
`canada.json` from the serde-rs json-benchmark suite).

Source: [`benches/compare.rs`](benches/compare.rs). Fixtures:
[`benches/fixtures/`](benches/fixtures/).

## Recommended pairing

`datavalue-rs` ships two value types — pick the one that matches the
workload, and that's what the tables below benchmark:

| Workload | Recommended type | Why |
|---|---|---|
| Parse, serialize, access | **`DataValue`** (arena-bound) | Zero-allocation hot path; `Copy` payloads; no per-node heap traffic. |
| Mutation | **`OwnedDataValue`** (heap-owned) | `DataValue` is immutable by design (the arena is the unit of mutation). `OwnedDataValue` mutates in place via `String` / `Vec`. |

The four result tables surface only the recommended variant per workload —
the parse / serialize / access tables show `datavalue`, the mutate table
shows `OwnedDataValue`. This is the pattern users should follow in their
own code.

<!-- BENCH:updated:BEGIN -->
_Last updated: 2026-05-04 (auto-generated by `scripts/update_benchmarks.py`)._
<!-- BENCH:updated:END -->

The four result tables below are regenerated by
[`scripts/update_benchmarks.py`](scripts/update_benchmarks.py); narrative
between them is preserved across runs. Run the script to refresh:

```bash
python3 scripts/update_benchmarks.py            # full run, ~10 min
python3 scripts/update_benchmarks.py --quick    # short smoke run
```

## Reproduce

```bash
cargo bench --bench compare --features serde_json
# Short smoke run:
cargo bench --bench compare --features serde_json -- --quick
```

Criterion writes an HTML report under `target/criterion/report/index.html`.

## Hardware

Numbers below were measured on Apple Silicon (`darwin/aarch64`), release
build, single thread, criterion default settings (3 s warm-up, 5 s
measurement). Times shown are medians.

## Parse — JSON string → DOM

Input throughput; higher is better.

<!-- BENCH:parse:BEGIN -->
| Library | twitter (631 KB) | citm (1.65 MB) | canada (2.15 MB) |
|---|---|---|---|
| datavalue | 502.56 µs · 1.1703 GiB/s | 1.2449 ms · 1.2921 GiB/s | 4.0508 ms · 529.96 MiB/s |
| serde_json::Value | 1.3927 ms · 432.45 MiB/s | 2.8604 ms · 575.85 MiB/s | 4.8739 ms · 440.46 MiB/s |
| simd_json (borrowed) | 520.46 µs · 1.1301 GiB/s | 1.4146 ms · 1.1371 GiB/s | 4.2392 ms · 506.41 MiB/s |
| simd_json (owned) | 879.50 µs · 684.78 MiB/s | 1.9575 ms · 841.49 MiB/s | 4.2462 ms · 505.57 MiB/s |
| **sonic_rs::Value** | **296.79 µs · 1.9817 GiB/s** | **688.46 µs · 2.3365 GiB/s** | **1.9487 ms · 1.0758 GiB/s** |
| json-rust | 838.07 µs · 718.63 MiB/s | 1.9821 ms · 831.04 MiB/s | 3.1849 ms · 674.04 MiB/s |
<!-- BENCH:parse:END -->

`sonic_rs` wins outright across all three fixtures. `datavalue` parse is
the second-fastest tier — ahead of both simd_json modes on twitter and
citm, and roughly tied with simd_json on the float-heavy canada fixture.

## Serialize — DOM → JSON string

Output throughput; higher is better.

<!-- BENCH:serialize:BEGIN -->
| Library | twitter | citm | canada |
|---|---|---|---|
| datavalue | 323.85 µs · 1.3427 GiB/s | 418.48 µs · 1.1134 GiB/s | 2.2292 ms · 882.79 MiB/s |
| serde_json::Value | 348.36 µs · 1.2482 GiB/s | 475.99 µs · 1002.4 MiB/s | 2.4326 ms · 808.97 MiB/s |
| simd_json (owned) | 299.19 µs · 1.4534 GiB/s | 504.28 µs · 946.14 MiB/s | 2.4866 ms · 791.40 MiB/s |
| sonic_rs::Value | 301.89 µs · 1.4404 GiB/s | 563.50 µs · 846.72 MiB/s | 3.0113 ms · 653.49 MiB/s |
| **json-rust** | **293.15 µs · 1.4833 GiB/s** | **403.09 µs · 1.1559 GiB/s** | **1.4443 ms · 1.3306 GiB/s** |
<!-- BENCH:serialize:END -->

Tight cluster — emit cost is dominated by string formatting. `datavalue`
sits in the middle of the pack, ahead of `serde_json`. Serialize is
treated as a read-only workload (you emit a parsed tree without mutating
it), so this row uses arena `DataValue` via `serde_json::to_string` over
its `Serialize` impl.

## Access — walk a representative path

Lower is better. Each fixture's walk shape:

- **twitter** — sum `screen_name` length + `retweet_count` over `statuses[]`
- **citm** — iterate `events` object, accumulate `id` + `subTopicIds.len()` per event
- **canada** — sum vertex counts across `features[].geometry.coordinates[]`

<!-- BENCH:access:BEGIN -->
| Library | twitter (statuses[].user, retweet_count) | citm (events.* id + subTopicIds) | canada (features[].coordinates[]) |
|---|---|---|---|
| **datavalue** | **1.9676 µs** | **886.80 ns** | **181.20 ns** |
| serde_json::Value | 6.0899 µs | 5.9513 µs | 189.88 ns |
| simd_json (owned) | 2.9510 µs | 1.2073 µs | 181.51 ns |
| sonic_rs::Value | 9.3140 µs | 9.7401 µs | 2.3484 µs |
| json-rust | 5.1677 µs | 4.6620 µs | 218.09 ns |
<!-- BENCH:access:END -->

`datavalue` leads twitter and citm walks by 3–10×. Notable inversion:
`sonic_rs` parses fastest but is the slowest accessor (its `Value` is
lazy / SIMD-backed; walking it isn't a flat-slice scan). The canada walk
is shallow enough that everyone except sonic_rs ties.

## Mutate — increment retweet_count over twitter.statuses[]

Only `twitter` is exercised here — the patch is a single, well-defined
edit and that's enough to compare mutation costs across DOM shapes.

This is the one workload where `OwnedDataValue` is the recommended
datavalue-rs variant — see the pairing table at the top.

<!-- BENCH:mutate:BEGIN -->
| Library | time | thrpt |
|---|---|---|
| OwnedDataValue (clone + mutate) | 283.55 µs | 2.0742 GiB/s |
| serde_json (clone + mutate) | 378.90 µs | 1.5522 GiB/s |
| simd_json (clone + mutate) | 289.31 µs | 2.0329 GiB/s |
| **json-rust (clone + mutate)** | **127.44 µs** | **4.6152 GiB/s** |
<!-- BENCH:mutate:END -->

All four rows clone the DOM at the start of each iteration, then mutate
the clone in place. `json-rust`'s mutation path is uniquely cheap;
`OwnedDataValue` and `simd_json` clone+mutate in roughly the same ballpark.
`sonic_rs` is omitted — its mutation surface differs enough that fitting a
fair comparable patch in is a follow-up.

## Methodology Notes

- The `datavalue` parse row uses a **reused arena** (`Bump::reset()` between
  iterations) — the workflow this crate is designed for. A separate
  `datavalue (fresh arena)` bench still runs in `benches/compare.rs` for
  completeness; on these fixture sizes the two are within noise (the
  first-chunk allocation is one syscall and disappears against a 500 µs+
  parse). The reused-arena win is meaningful only for sub-microsecond
  parses or large batches.
- All benches use `criterion::black_box` on inputs and outputs.
- `simd_json` requires a mutable byte buffer — each iteration gets a
  fresh `Vec<u8>` via `iter_batched(BatchSize::SmallInput)`, so the bench
  measures parse cost, not buffer reuse.
- The `datavalue` serialize row goes through `serde_json::to_string` over
  its `Serialize` impl — the crate doesn't expose a native string emitter.
- `sonic_rs::Value` and `simd_json::OwnedValue` both implement traits
  from the shared `value-trait` crate, which collide on `sonic_rs::Value`
  with `sonic_rs::JsonValueTrait`. The bench imports `simd_json::prelude`
  only inside simd_json bench bodies to avoid the ambiguity.

## Versions Pinned

See [`Cargo.toml`](Cargo.toml). Currently:
`simd-json 0.13`, `sonic-rs 0.3`, `json 0.12`, `serde_json 1.0`,
`criterion 0.5`.
